#ifndef SATURATION_H
#define SATURATION_H
#include <opencv\cv.h>
#include <opencv\cxcore.h>
#include <opencv\highgui.h>
class Saturation{
public:
	void saturation(cv::Mat &model, double variable);
private:
};
#endif